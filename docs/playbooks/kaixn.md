# Engineering playbook — .

> 17 axes · exact deterministic support + real-LLM design pass · threshold 80%.

| axis | value | support | tier | method | conv? | evidence / counterexamples |
|---|---|---|---|---|---|---|
| naming-case | snake_case | 176/176 (100%) | advisory | deterministic | ✅ | `scripts/apply_migrations.py:main`; `scripts/dogfood.py:banner`; `scripts/dogfood.py:main` |
| module-docstrings | present | 24/24 (100%) | advisory | deterministic | ✅ | `scripts/apply_migrations.py`; `scripts/dogfood.py`; `src/kaixn/bootstrap.py` |
| future-annotations | all-modules | 23/24 (96%) | advisory | deterministic | ✅ | `src/kaixn/__init__.py` |
| dataclass-slots | slots=True | 23/25 (92%) | advisory | deterministic | ✅ | `src/kaixn/server.py:AppState`; `src/kaixn/app.py:Kaixn` |
| type-annotations | full | 121/137 (88%) | advisory | deterministic | ✅ | `scripts/dogfood.py:banner`; `scripts/dogfood.py:adjudicate`; `scripts/dogfood.py:assess_coverage` |
| test-mirroring | mirrored | 8/23 (35%) | governed | deterministic | — | `scripts/apply_migrations.py:no test_apply_migrations.py`; `scripts/dogfood.py:no test_dogfood.py`; `src/kaixn/waitlist.py:no test_waitlist.py` |
| public-surface | leading-underscore-private | 28/95 (29%) | advisory | deterministic | — | `src/kaixn/bootstrap.py:_commit_candidate`; `src/kaixn/waitlist.py:_file_path`; `src/kaixn/waitlist.py:_add_file` |
| layering-direction | Layered with a clear dependency direction: types → similarity/embedding → store (seam) → gate/engine/conflict → app/server/web. Higher layers depend on lower-layer protocols/seams; no upward imports detected. | 6/6 (100%) | advisory | llm-verified | ✅ | `src/kaixn/types.py has no kaixn imports`; `src/kaixn/store.py imports from kaixn.embedding and kaixn.types only`; `src/kaixn/gate.py imports from kaixn.llm, kaixn.similarity, kaixn.store, kaixn.embedding, kaixn.types` |
| seam-pattern | typing.Protocol is the primary seam mechanism, used for NormReader, Embedder, Synthesizer, AtomicitySplitter, Adjudicator, NormExtractor, and Grounder. Concrete classes satisfy the protocol structurally (duck-typed conformance); no ABC inheritance is used. | 6/6 (100%) | advisory | llm-verified | ✅ | `src/kaixn/store.py: `class NormReader(Protocol)` with neighbors/active_principles/get`; `src/kaixn/embedding.py: `@runtime_checkable class Embedder(Protocol)``; `src/kaixn/engine.py: `class Synthesizer(Protocol)` with synthesize()` |
| dependency-injection | Constructor injection is the dominant pattern: all seam implementations (Embedder, NormReader, AtomicitySplitter, Adjudicator) are passed into consumers at construction time. Top-level wiring happens in Kaixn.from_env() and AppState.__post_init__(), which read env-vars to select concrete implementations. No global singletons except the module-level STATE in server.py. | 6/6 (100%) | advisory | llm-verified | ✅ | `src/kaixn/gate.py: WriteGate.__init__(reader, embedder, splitter, adjudicator, config)`; `src/kaixn/conflict.py: ConflictEngine.__init__(reader, adjudicator, embedder)`; `src/kaixn/app.py: Kaixn.from_env() reads KAIXN_DSN / KAIXN_EMBEDDER / ANTHROPIC_API_KEY to select impls` |
| error-signaling | Failures are signaled by raising typed exceptions (ValueError for bad input, RuntimeError for infrastructure failures) at trust boundaries. Internal engine decisions use typed enum return values (GateDecision, Verdict, OpStatus) rather than exceptions. HTTP layer translates exceptions to HTTPException with appropriate status codes. | 6/6 (100%) | advisory | llm-verified | ✅ | `src/kaixn/app.py: normalize_repo_url raises ValueError for invalid URLs`; `src/kaixn/web.py: connect() catches ValueError -> 400, RuntimeError -> 502`; `src/kaixn/types.py: GateDecision enum (ACCEPT/SPLIT/MERGE_CANDIDATE/CONFLICT) as typed result` |
| input-validation | Untrusted input is validated at trust boundaries: HTTP request bodies via Pydantic BaseModel (FastAPI), URL validation in normalize_repo_url() before any network call, email validation in waitlist.valid_email() before storage, and scope normalization via normalize_scope() at NormCandidate construction. LLM JSON output is parsed defensively with try/except fallbacks. | 6/6 (100%) | governed | llm-verified | ✅ | `src/kaixn/web.py: Pydantic BaseModel classes (ConnectBody, IntentBody, etc.) validate all API inputs`; `src/kaixn/app.py: normalize_repo_url() validates and normalizes before git clone`; `src/kaixn/waitlist.py: valid_email() gating before any storage write` |
| state-mutation | Append-only / status-flip for the norm store: norms are never mutated in place; supersede/modify operations create new norm records and flip the old norm's status field. In-memory store uses list append. The resolution module's docstring explicitly states 'append-only: never mutate in place'. Proposals dict is mutable in-process (transient state). | 4/4 (100%) | governed | llm-verified | ✅ | `src/kaixn/resolution.py: docstring 'append-only: never mutate in place' for modify/supersede`; `src/kaixn/store.py: InMemoryStore._norms is a list; add/add_norm append only`; `src/kaixn/waitlist.py: _add_file opens with mode 'a' (append); Postgres uses INSERT ON CONFLICT DO NOTHING` |
| concurrency-model | Synchronous with one explicit thread-safety measure: waitlist file writes use a module-level threading.Lock. The web server uses FastAPI (ASGI/async-capable) but all route handlers and service methods are synchronous. The in-memory store has no locking. LLM calls are synchronous blocking (Anthropic SDK). No async/await in the application layer. | 5/5 (100%) | advisory | llm-verified | ✅ | `src/kaixn/waitlist.py: `_file_lock = threading.Lock()` guards file appends`; `src/kaixn/web.py: all @app.get/@app.post handlers are sync `def`, not `async def``; `src/kaixn/store.py: InMemoryStore list operations — no locks` |
| trust-boundary | External input is validated and normalized at well-defined trust boundaries: HTTP API inputs via Pydantic, GitHub URLs via regex before cloning, email addresses via regex+length before storage, ltree scopes normalized at dataclass construction, and LLM-generated JSON parsed defensively. The miner treats repo files as untrusted (encoding errors ignored, SyntaxErrors caught). | 6/6 (100%) | governed | llm-verified | ✅ | `src/kaixn/web.py: Pydantic BaseModel enforces types on all POST bodies before service methods are called`; `src/kaixn/app.py: normalize_repo_url() — strict regex, ValueError on anything non-GitHub before network call`; `src/kaixn/waitlist.py: valid_email() + length check before any write` |
| data-access | A NormReader Protocol seam abstracts all constitution reads. InMemoryNormReader provides the in-process implementation; PgStore (referenced but not shown) is the production implementation. Callers (gate, conflict engine, drift reviewer) depend only on the NormReader protocol. No inline SQL appears in application logic; SQL is isolated to migration scripts and PgStore. | 5/6 (83%) | governed | llm-verified | ✅ | `src/kaixn/resolution.py:commit_proposal accepts `store: InMemoryStore` directly rather than depending on` |
| offline-fallback | Every LLM-backed component has a deterministic offline fallback: NaiveSynthesizer for AnthropicSynthesizer, HeuristicSplitter for AnthropicSplitter, HeuristicExtractor for AnthropicExtractor, _ConsistentAdjudicator for AnthropicAdjudicator, FakeEmbedder for cloud embedders, RepoGrounder for AnthropicGrounder. The switch is driven by ANTHROPIC_API_KEY presence. | 4/6 (67%) | governed | llm-verified | — | `src/kaixn/app.py:The file imports AnthropicSynthesizer and NaiveSynthesizer and defines _Consiste`; `src/kaixn/embedding.py:The file defines FakeEmbedder (the required offline fallback for cloud embedders` |
