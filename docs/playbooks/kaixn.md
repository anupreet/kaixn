# Engineering playbook — .

> 17 axes · exact deterministic support + real-LLM design pass · threshold 80%.

| axis | value | support | tier | method | conv? | evidence / counterexamples |
|---|---|---|---|---|---|---|
| naming-case | snake_case | 156/156 (100%) | advisory | deterministic | ✅ | `scripts/apply_migrations.py:main`; `scripts/dogfood.py:banner`; `scripts/dogfood.py:main` |
| module-docstrings | present | 22/22 (100%) | advisory | deterministic | ✅ | `scripts/apply_migrations.py`; `scripts/dogfood.py`; `src/kaixn/bootstrap.py` |
| future-annotations | all-modules | 21/22 (95%) | advisory | deterministic | ✅ | `src/kaixn/__init__.py` |
| dataclass-slots | slots=True | 23/25 (92%) | advisory | deterministic | ✅ | `src/kaixn/server.py:AppState`; `src/kaixn/app.py:Kaixn` |
| type-annotations | full | 112/128 (88%) | advisory | deterministic | ✅ | `scripts/dogfood.py:banner`; `scripts/dogfood.py:adjudicate`; `scripts/dogfood.py:assess_coverage` |
| test-mirroring | mirrored | 7/21 (33%) | governed | deterministic | — | `scripts/apply_migrations.py:no test_apply_migrations.py`; `scripts/dogfood.py:no test_dogfood.py`; `src/kaixn/store.py:no test_store.py` |
| public-surface | leading-underscore-private | 17/75 (23%) | advisory | deterministic | — | `src/kaixn/bootstrap.py:_commit_candidate`; `src/kaixn/store.py:_vec`; `src/kaixn/miner.py:_py_files` |
| layering-direction | callers toward a seam — strict inward dependency direction: web/server → app → engine/gate/conflict/resolution → store (Protocol seam) → types/similarity/embedding. No upward imports detected. | 10/10 (100%) | governed | llm | ✅ | `src/kaixn/web.py`; `src/kaixn/app.py`; `src/kaixn/server.py` |
| seam-pattern | typing.Protocol throughout — NormReader, Synthesizer, NormExtractor, AtomicitySplitter, Adjudicator, Embedder, Grounder are all defined as typing.Protocol; concrete classes satisfy them structurally (duck-typed), not via ABC inheritance. | 10/10 (100%) | governed | llm | ✅ | `src/kaixn/store.py (class NormReader(Protocol))`; `src/kaixn/engine.py (class Synthesizer(Protocol))`; `src/kaixn/extract.py (class NormExtractor(Protocol))` |
| dependency-injection | constructor injection at every seam; a single env-switch factory (get_embedder, Kaixn.from_env) assembles the graph at process startup. No global mutable singletons in library code; server.py has one module-level STATE = AppState() for the POC path. | 10/10 (100%) | governed | llm | ✅ | `src/kaixn/gate.py (WriteGate.__init__ takes reader, embedder, splitter, adjudicator)`; `src/kaixn/conflict.py (ConflictEngine.__init__ takes reader, adjudicator, embedder)`; `src/kaixn/review.py (DriftReviewer.__init__ takes store, adjudicator, embedder)` |
| offline-fallback | every LLM path has an explicit deterministic offline fallback: NaiveSynthesizer for AnthropicSynthesizer, HeuristicSplitter for AnthropicSplitter, HeuristicExtractor for AnthropicExtractor, _ConsistentAdjudicator for AnthropicAdjudicator, FakeEmbedder for Ollama/OpenAI, RepoGrounder for AnthropicGrounder. | 10/10 (100%) | governed | llm | ✅ | `src/kaixn/engine.py (NaiveSynthesizer — offline fallback for synthesis)`; `src/kaixn/llm.py (HeuristicSplitter — offline fallback for atomicity splitting)`; `src/kaixn/extract.py (HeuristicExtractor — offline fallback for norm extraction)` |
| error-signaling | typed exceptions raised at boundaries (ValueError for bad input, RuntimeError for infrastructure failures, HTTPException in web layer); internal logic uses structured return types (GateResult, ConflictReport, CommitResult, ReviewReport) with discriminated enum fields rather than exceptions for expected variant outcomes. | 10/10 (100%) | governed | llm | ✅ | `src/kaixn/app.py (normalize_repo_url raises ValueError; connect_repo raises RuntimeError for git failure)`; `src/kaixn/web.py (catches ValueError→400, RuntimeError→502, re-raises as HTTPException)`; `src/kaixn/types.py (GateDecision enum: ACCEPT/SPLIT/MERGE_CANDIDATE/CONFLICT as return variant)` |
| input-validation | validated at the trust boundary — URL normalization and GitHub pattern validation in app.py before any network call; scope normalization in types.py NormCandidate.__post_init__; Pydantic models validate HTTP request bodies in web.py; empty-intent guard in web.py before reaching service logic. | 10/10 (100%) | governed | llm | ✅ | `src/kaixn/app.py (normalize_repo_url: strip, scheme normalization, _GITHUB_RE.match before clone)`; `src/kaixn/types.py (NormCandidate.__post_init__ calls normalize_scope on every construction)`; `src/kaixn/types.py (normalize_scope: coerces any string to valid ltree path)` |
| data-access | reader/repository seam — NormReader Protocol defines the access contract (neighbors, active_principles, get); all business logic depends on the Protocol; InMemoryStore and PgStore are concrete implementations behind the seam. | 10/10 (100%) | governed | llm | ✅ | `src/kaixn/store.py (class NormReader(Protocol) with neighbors, active_principles, get)`; `src/kaixn/gate.py (WriteGate takes reader: NormReader — no DB knowledge)`; `src/kaixn/conflict.py (ConflictEngine takes reader: NormReader)` |
| concurrency-model | synchronous Python throughout the core library; async only at the HTTP boundary (FastAPI/uvicorn in web.py); no threading, no asyncio in business logic. InMemoryStore has no locks — not race-safe for concurrent writes, but scoped to single-process POC use. | 10/10 (100%) | advisory | llm | ✅ | `src/kaixn/web.py (FastAPI app — async HTTP framework, but route handlers are def not async def)`; `src/kaixn/store.py (InMemoryStore: plain list append, no locks, no asyncio)`; `src/kaixn/gate.py (all synchronous)` |
| trust-boundary | external input is validated and normalized at the trust boundary: HTTP bodies via Pydantic in web.py, URL inputs normalized+validated in app.py before any network call, ltree scopes coerced to valid paths in NormCandidate.__post_init__, LLM-produced JSON parsed defensively with try/except fallbacks. | 10/10 (100%) | governed | llm | ✅ | `src/kaixn/app.py (normalize_repo_url validates and normalizes before git clone)`; `src/kaixn/web.py (Pydantic BaseModel on all POST bodies; explicit 400 for empty intent)`; `src/kaixn/types.py (normalize_scope coerces LLM-produced scope strings at NormCandidate construction)` |
| state-mutation | append-only for norm records (norms are never updated in place; status changes are modeled as field flips on immutable-by-convention records, with supersede/deprecate producing new status rather than mutating statement); in-process collections (InMemoryStore._norms list, proposals dict) are mutated by append/assignment but existing records are not overwritten. | 7/10 (70%) | advisory | llm | — | `src/kaixn/resolution.py ('modify → same mechanics as supersede (append-only: never mutate in place)')`; `src/kaixn/store.py (InMemoryStore.add_norm appends to self._norms; no update/delete method visible)`; `src/kaixn/types.py (NormRecord has slots=True; status field is the only mutable state, flipped for supersede/deprecate)` |
