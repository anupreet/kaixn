# Seed eval set for the conflict engine (op_statement × norm → expected verdict).
# Grow this from every human-confirmed verdict in production. Run the real model:
#   python -m kaixn.eval evals/conflict_cases.jsonl   (needs ANTHROPIC_API_KEY)
{"op_statement": "Delete the account immediately with no undo window", "norm": {"id": "ux1", "statement": "All destructive user actions must be reversible", "domain": "ux", "scope": "all", "kind": "principle"}, "expected": "conflict"}
{"op_statement": "Add a 30-day soft-delete with a restore button", "norm": {"id": "ux1", "statement": "All destructive user actions must be reversible", "domain": "ux", "scope": "all", "kind": "principle"}, "expected": "consistent"}
{"op_statement": "Fetch the user's full order history synchronously before first paint", "norm": {"id": "t1", "statement": "Never block the main thread on a network call", "domain": "technical", "scope": "all", "kind": "principle"}, "expected": "conflict"}
{"op_statement": "Cache the dashboard server-side for 60 seconds", "norm": {"id": "t1", "statement": "Never block the main thread on a network call", "domain": "technical", "scope": "all", "kind": "principle"}, "expected": "consistent"}
{"op_statement": "Process card payments through Adyen", "norm": {"id": "d1", "statement": "Billing uses Stripe for card processing", "domain": "technical", "scope": "all.product.billing", "kind": "decision"}, "expected": "conflict"}
{"op_statement": "Add a new webhook for Stripe refund events", "norm": {"id": "d1", "statement": "Billing uses Stripe for card processing", "domain": "technical", "scope": "all.product.billing", "kind": "decision"}, "expected": "consistent"}
{"op_statement": "Show prices excluding tax on the pricing page", "norm": {"id": "p1", "statement": "Prices shown to EU users must include VAT", "domain": "product", "scope": "all.product.pricing", "kind": "principle"}, "expected": "tension"}
{"op_statement": "Let users export their data as CSV", "norm": {"id": "p2", "statement": "Users must be able to delete all their data on request", "domain": "product", "scope": "all", "kind": "principle"}, "expected": "consistent"}
{"op_statement": "Store passwords using SHA-1 without a salt", "norm": {"id": "t2", "statement": "Credentials must be stored with a slow salted hash (bcrypt/argon2)", "domain": "technical", "scope": "all.security", "kind": "principle"}, "expected": "conflict"}
{"op_statement": "Hash passwords with argon2id", "norm": {"id": "t2", "statement": "Credentials must be stored with a slow salted hash (bcrypt/argon2)", "domain": "technical", "scope": "all.security", "kind": "principle"}, "expected": "consistent"}
{"op_statement": "Log full credit-card numbers for debugging", "norm": {"id": "t3", "statement": "Never log PII or secrets in plaintext", "domain": "technical", "scope": "all", "kind": "principle"}, "expected": "conflict"}
{"op_statement": "Log only the last four digits of the card", "norm": {"id": "t3", "statement": "Never log PII or secrets in plaintext", "domain": "technical", "scope": "all", "kind": "principle"}, "expected": "consistent"}
{"op_statement": "Ship the new dashboard with no loading or error states", "norm": {"id": "ux2", "statement": "Every async view must render loading and error states", "domain": "ux", "scope": "all", "kind": "principle"}, "expected": "conflict"}
{"op_statement": "Add skeleton loaders and an error retry banner to the dashboard", "norm": {"id": "ux2", "statement": "Every async view must render loading and error states", "domain": "ux", "scope": "all", "kind": "principle"}, "expected": "consistent"}
{"op_statement": "Use a custom modal that traps keyboard focus permanently", "norm": {"id": "ux3", "statement": "Interactive components must be keyboard accessible and escapable", "domain": "ux", "scope": "all", "kind": "principle"}, "expected": "conflict"}
{"op_statement": "Make the onboarding flow 8 mandatory steps before first value", "norm": {"id": "pr1", "statement": "Users must reach first value within 3 steps", "domain": "product", "scope": "all.product.onboarding", "kind": "principle"}, "expected": "conflict"}
{"op_statement": "Let users skip onboarding and explore immediately", "norm": {"id": "pr1", "statement": "Users must reach first value within 3 steps", "domain": "product", "scope": "all.product.onboarding", "kind": "principle"}, "expected": "consistent"}
{"op_statement": "Store all timestamps in local server time", "norm": {"id": "t4", "statement": "All persisted timestamps must be UTC", "domain": "technical", "scope": "all", "kind": "principle"}, "expected": "conflict"}
{"op_statement": "Persist created_at as UTC and convert in the UI", "norm": {"id": "t4", "statement": "All persisted timestamps must be UTC", "domain": "technical", "scope": "all", "kind": "principle"}, "expected": "consistent"}
{"op_statement": "Roll out the redesign to 100% of users on day one", "norm": {"id": "pr2", "statement": "Major UI changes ship behind a feature flag with gradual rollout", "domain": "product", "scope": "all", "kind": "principle"}, "expected": "conflict"}
{"op_statement": "Use a different accent color just on the settings page", "norm": {"id": "pd1", "statement": "Use design-system tokens for all colors", "domain": "product_design", "scope": "all", "kind": "tension"}, "expected": "tension"}
{"op_statement": "Add a settings page using design-system color tokens", "norm": {"id": "pd1", "statement": "Use design-system tokens for all colors", "domain": "product_design", "scope": "all", "kind": "principle"}, "expected": "consistent"}
{"op_statement": "Call the payments API directly from the browser with the secret key", "norm": {"id": "t5", "statement": "Secret keys must only be used server-side", "domain": "technical", "scope": "all.security", "kind": "principle"}, "expected": "conflict"}
{"op_statement": "Add a rate limiter in front of the public API", "norm": {"id": "t6", "statement": "Public endpoints should be rate-limited", "domain": "technical", "scope": "all", "kind": "principle"}, "expected": "consistent"}
